import SwiftUI

// Phase 4 (Pro): condition slider → expected PSA grade → net profit after fee
struct GradeROIView: View {
    let card: Card

    var body: some View {
        Text("Grade ROI — Phase 4 (Pro)")
            .navigationTitle("Grade ROI")
    }
}
