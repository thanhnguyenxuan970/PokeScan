import SwiftUI

// Phase 3: server-synced collection list
struct CollectionView: View {
    var body: some View {
        Text("Collection — Phase 3")
            .navigationTitle("Collection")
    }
}
