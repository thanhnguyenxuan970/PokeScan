package com.snapdex.app.ui.theme

import androidx.compose.ui.graphics.Color

// Brand
val SnapDexBlue = Color(0xFF2563EB)      // accent/primary
val SnapDexBlueDark = Color(0xFF1D4ED8)  // accent/pressed
val SnapDexBlueTint = Color(0xFFEFF4FE)  // accent/tint

// Semantic
val SnapDexDanger = Color(0xFFDC2626)    // danger token
val SnapDexSuccess = Color(0xFF16A34A)   // success token
val SnapDexGold = Color(0xFFD97706)      // warning token

// Icon-specific (Pokéball red — not a semantic token)
val SnapDexRed = Color(0xFFEE1515)

// Text
val SnapDexBlack = Color(0xFF0A0A0A)         // text/primary
val SnapDexGray = Color(0xFF6E6E73)          // text/secondary
val SnapDexTextTertiary = Color(0xFFAEAEB2)  // text/tertiary

// Surfaces
val SnapDexSurface = Color(0xFFFFFFFF)   // bg/canvas
val SnapDexWhite = Color(0xFFFAFAFA)     // bg/elevated
val SnapDexSunken = Color(0xFFF2F2F7)    // bg/sunken

// Borders
val SnapDexBorderHairline = Color(0xFFE5E5EA)  // border/hairline
val SnapDexBorderDefault = Color(0xFFD1D1D6)   // border/default
